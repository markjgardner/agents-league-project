# GitHub token for creating issues (automatically available in GitHub Actions)
# For local use, create a personal access token with "repo" scope
GITHUB_TOKEN=

# Optional: Override repo root path
# REDTEAM_REPO_ROOT=.

# Optional: Set log level (debug, info, warn, error)
# LOG_LEVEL=info

# Optional: Set persona for issue writing style ("default" or "chuck_norris")
# REDTEAM_PERSONA=default

# ─── LLM Attack Planner (optional) ─────────────────────────────────
# Enable the LLM-based hypothesis planner (default: false)
# REDTEAM_LLM_ENABLED=false

# LLM model to use (default: gpt-4o-mini)
# REDTEAM_LLM_MODEL=gpt-4o-mini

# LLM API endpoint (optional — defaults to OpenAI API)
# REDTEAM_LLM_ENDPOINT=https://api.openai.com/v1

# LLM API key (required when LLM planner is enabled)
# REDTEAM_LLM_KEY=

# Max files to send to LLM for analysis (default: 20)
# REDTEAM_LLM_MAX_FILES=20

# Max tokens for LLM response (default: 4096)
# REDTEAM_LLM_MAX_TOKENS=4096

# Comma-separated list of allowed hostnames for dynamic probes (default: localhost,127.0.0.1,::1)
# REDTEAM_TARGET_ALLOWLIST=localhost,127.0.0.1,::1
